# AGENTS — hierarchical-nc repository adapter

Before non-trivial work, read and follow `geonwooo/Continual-AI-System@main:AI_START_HERE.md`, then read this repository's `AI_CONTEXT_LINKS.yaml` for semantic ownership and cross-repository decision-lineage rules.

GitHub remote state is canonical. This repository owns **hierarchical Neural Collapse domain research assets** assigned here and is a consolidation candidate. Before writing, determine whether the active mission declares this repository, `nc-longtail`, or `Research` as the canonical owner. Never create a second authoritative copy of the same protocol, code, or result.

Inspect existing instructions, exact branch/SHA, open PRs, experiment state, data boundaries, receipts, and claim status. Preserve negative results, confounds, reproducibility metadata, and publication history. Link to upstream/downstream assets by exact SHA rather than copying mutable summaries.

When local evidence changes a mission-level judgment, preserve it here and reference its exact commit from the owning mission lineage instead of duplicating mission state.

Route reusable failures to AI-Stack and active mission state to ai-god. Commit and push non-trivial work and leave the central continuity receipt.

Do not consolidate, delete, rewrite history, access restricted data, promote claims, publish, merge, spend, or deploy without explicit Principal approval.
